# 5.5.1　BatteryService分析


Ba eryService由System Server创建，代码如下：

```
ba ery=new Ba eryService(context,
li ghts);
ServiceManager.addService("ba ery",
ba ery);
```

下面来看Ba eryService的构造函数：

[--＞Ba eryService.java：Ba ery
Service]

```java
publi c Ba eryService(Context
context, LightsService li ghts){
mContext=context;
mLed=new Led(context, li ghts);//提
```

示灯控制，感兴趣的读者可自行阅读相关代码

```
//Ba eryService也需要和
Ba eryStatsService交互
mBa eryStats=Ba eryStatsService.getS
ervice();
//获取一些配置参数
mCriti calBa eryLevel=mContext.getReso
urces().getInteger(
com.android.internal.R.integer.confi g_
criti calBa eryWarningLevel);
mLowBa eryWarningLevel=mContext.getRe
sources().getInteger(
cifom.android.internal.R.integer.confi g_
lowBa eryWarningLevel);
mLowBa eryCloseWarningLevel=mContext.
getResources().getInteger(
com.android.internal.R.integer.confi g_
lowBa eryCloseWarningLevel);
//启动uevent监听对象,监视power_supply
信息
mPowerSupplyObserver.startObserving
("SUBSYSTEM=power_supply");
//如果下列文件存在,那么启动另一个
```

uevent监听对象。该uevent事件来自

```
invali d charger
//switch设备(即不匹配的充电设备)
(new Fil e
("/sys/devices/virtual/swit ch/invali d
_charger/state").exists()){
mInvali dChargerObserver.startObserving
(
"DEVPATH=/devices/virtual/swit ch/inva
li d_charger");
}
```

update（）；//①查询HAL层，获取此时的电池信息

```
}
```

Ba eryService定义了3个非常重要的阈值，分别是：

m Criti calBa eryLevel表示严重低电，其值为4。当电量低于该值时会强制关机。该值由config.xm l中的config_criti calBa eryW arningLevel控制。

m LowBa eryW arningLevel表示低电，值为15，当电量低于该值时，系统会报警，例如闪烁LED灯。该值由config.xml中的config_lowBa eryW arningLevel控制。

m LowBa eryCloseW arningLevel表示一旦电量大于此值，就脱离低电状态，即可停止警示灯。该值为20，由config.xml中的config_lowBa eryCloseW arningLevel控制。在BaeryService构造函数的最后调用了update函数，该函数将查询系统电池信息，以更新BaeryService内部的成员变量。此函数代码如下：

[--＞Ba eryService.java：update]

```java
private synchronized fi nal void update
(){
```

nati ve_update（）；//到Nati ve层查询并更新内部变量的值processValues（）；//处理更新后的状态

```
}
```

1.native_update函数分析native_update的实现代码如下：

[--＞

com _android_server_Ba eryService.cpp：

```cpp
android_server_Ba eryService_update]
stati c void
android_server_Ba eryService_update
(JNIEnv*env, jobject obj)
{
setBooleanField(env, obj,
gPaths.acOnli nePath,
gFieldIds.mAcOnli ne);
```

……//获取电池信息，并通过JNI设置到Java层对应的变量中

```
setIntField(env, obj,
gPaths.ba eryTemperaturePath,
gFieldIds.mBa eryTemperature);
const int SIZE=128;
char buf[SIZE];
//获取信息,以下参数并不是所有手机都支
持的
if(readFromFil e
(gPaths.ba eryStatusPath, buf,
SIZE)>0)
env->SetIntField(obj,
gFieldIds.mBa eryStatus,
getBa eryStatus(buf));
else
env->SetIntField(obj,
gFieldIds.mBa eryStatus,
gConstants.statusUnknown);
……
}
```

Android系统中的电池信息如表5-4所示。

m Ba eryStatus和m Ba eryHealth均有几种不同状态，详细信息可查看getBa eryStatus和getBa eryHealth函数的实现。

上述信息均通过从/sys/class/pow er_supply目录读取对应文件得到。和以往使用固定路径（可能是Android2.2版本之前）不同的是，本处先读取power_supply目录中各个子目录中的type文件，然后根据type文件的内容，再做对应处理：

如果type文件的内容为Mains，则读取对应子目录中的online文件，可判断是否为AC充电。

如果type文件的内容为Baery，则从对应子目录中其他的文件中读取电池相关的信息，例如从temp文件获取电池温度，从technology文件读取电池制造技术等。

如果type文件的内容为USB，则读取该子目录中的online文件内容，可判断是否为USB充if电。

提示读者可通过dumpsysbaery查看自己手机的电池信息。

2.processValues分析获取了电池信息后，BaeryService就要做一些处理，此项工作通过processValues完成，其代码如下：

[--＞Ba eryService.java：processValues]

```java
private void processValues(){
long dischargeDurati on=0;
mBa eryLevelCriti cal=mBa eryLevel<
=mCriti calBa eryLevel;
(mAcOnli ne){
mPlugType=Ba eryManager.BATTERY_PLUGG
ED_AC;
}else if(mUsbOnli ne){
mPlugType=Ba eryManager.BATTERY_PLUGG
ED_USB;
}else{
mPlugType=BATTERY_PLUGGED_NONE;
}
//通知Ba eryStatsService,该函数以后
再分析
mBa eryStats.setBa eryState
(mBa eryStatus, mBa eryHealt h,
mPlugType, mBa eryLevel,
mBa eryTemperature, mBa eryVolt age
);
```

shutdownIfNoPower（）；//如果电量不够，则弹出关机对话框

```
shutdownIfOverTemp();//如果电池过
```

热，则弹出关机对话框……//根据当前电池信息与上次电池信息比较，判断是否需要发送广播等if（……/*比较前后两次电池信息是否发生变化*/）{……//记录信息到日志文件

```
Intent statusIntent=new Intent();
statusIntent.setFlags(
Intent.FLAG_RECEIVER_REGISTERED_ONLY_B
EFORE_BOOT);
if(mPlugType!=0&&
mLastPlugType==0){
statusIntent.setActi on
(Intent.ACTION_POWER_CONNECTED);
mContext.sendBroadcast
(statusIntent);
}……
if(sendBa eryLow){
mSentLowBa eryBroadcast=true;//发送
```

低电提醒

```
statusIntent.setActi on
(Intent.ACTION_BATTERY_LOW);
mContext.sendBroadcast
(statusIntent);
}……
mLed.updateLightsLocked();//更新LED
```

灯状态

```
mLastBa eryStatus=mBa eryStatus;//
```

保存新的电池信息

```
……
}
```

processValues函数非常简单，此处不再详述。另外，当电池信息发生改变时，系统会发送uevent事件给Ba eryService，此时Ba eryService只要重新调用update即可完成工作。
