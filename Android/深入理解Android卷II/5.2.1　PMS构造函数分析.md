# 5.2.1　PMS构造函数分析


PMS构造函数的代码如下：

[--＞PowerM anagerService.java：
Pow erM anagerService]

```java
PowerManagerService(){
long token=Binder.clearCalli ngIdentit y
();
```

MY_UID=Process.myUid（）；//取本进程

```
(即SystemServer)的uid及pid
MY_PID=Process.myPid();
Binder.restoreCalli ngIdentit y
(token);
//设置超时时间为1周。Power类封装了同
```

Linux内核交互的接口。本章最后再来分析它

```
Power.setLastUserActi vit yTimeout
(7*24*3600*1000);
//初始化两个状态变量,它们非常有意义。
```

其具体作用后续再分析

```
mUserState=mPowerState=0;
//将自己添加到看门狗(Watchdog)的监控
管理队列中
Watchdog.getInstance().addMonit or
(this);
}
```

PMS的构造函数比较简单。值得注意的是m UserState和m PowerState两个成员，至
